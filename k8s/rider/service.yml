apiVersion: v1
kind: Service
metadata:
  name: rider-service
  namespace: swiggy
spec:
  selector:
    app: rider-service
  ports:
    - port: 5005
      targetPort: 5005
  type: ClusterIP
